// Re-export from common/SideNav to preserve existing component
// The SideNav component lives in common/ due to its tight coupling with resource types
export { default } from '@/components/common/SideNav';
